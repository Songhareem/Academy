<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.song.Spring_legacy2.member.MemberDAO">
	
	<sql id="search">
		LIKE '%'||#{search}||'%'
	</sql>
	
	<select id="memberCount" parameterType="com.song.Spring_legacy2.member.memberPage.MemberPager" resultType="java.lang.Long">
		SELECT COUNT(rownum) FROM member
		<where>
			<if test="kind == 'id'">
				id <include refid="search"></include>
			</if>
			<if test="kind == 'name'">
				name <include refid="search"></include>
			</if>
			<if test="kind == 'email'">
				email <include refid="search"></include>
			</if>
			<if test="kind == 'phone'">
				phone <include refid="search"></include>
			</if>
		</where>
	</select>
	
	<select id="memberList" parameterType="com.song.Spring_legacy2.member.memberPage.MemberPager" resultType="com.song.Spring_legacy2.member.MemberVO">
		SELECT * FROM
		(SELECT M.*, rownum R FROM
		(SELECT * FROM member WHERE
		<choose>
			<when test="kind == 'name'">name</when>
			<when test="kind == 'email'">email</when>
			<when test="kind == 'phone'">phone</when>
			<otherwise>id</otherwise>
		</choose>
		LIKE '%'||#{search}||'%') M)
		WHERE R BETWEEN #{startRow} AND #{lastRow}
	</select>
	
	<select id="memberLogin" parameterType="com.song.Spring_legacy2.member.MemberVO" resultType="com.song.Spring_legacy2.member.MemberVO">
		SELECT * FROM member WHERE id=#{id} AND pw=#{pw}
	</select>
	
	<insert id="memberJoin" parameterType="com.song.Spring_legacy2.member.MemberVO">
		INSERT INTO member VALUES(#{id},#{pw},#{name},#{age},#{email},#{phone})
	</insert>
	
	<insert id="memberUpdate" parameterType="com.song.Spring_legacy2.member.MemberVO">
		UPDATE member SET pw=#{pw}, name=#{name}, age=#{age}, email=#{email}, phone=#{phone} WHERE id=#{id}
	</insert>
	
	<insert id="memberDelete" parameterType="com.song.Spring_legacy2.member.MemberVO">
		DELETE FROM member WHERE id = #{id}
	</insert>
</mapper>